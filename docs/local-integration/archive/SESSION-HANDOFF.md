# Historical local-integration record

> Public redaction notice

This file previously contained machine-specific paths, hardware topology, process
state, benchmark measurements, or recovery details. That operational evidence is
not part of the public OpenCodex contract and has been removed from this fork.

It does not define supported runtime behavior, release criteria, or a public
setup procedure. Public behavior is summarized in `README-FORK.md` and
`docs/local-integration/README.md`; machine-local launch and integrity details
belong only in ignored operator configuration.

Do not reintroduce raw command transcripts, host inventories, artifact hashes,
local filesystem layout, captured logs, or benchmark results into this record.
