# Public continuation boundary

Do not use this public repository to carry a private operator prompt or session
handoff. Public continuation notes may state the managed 192K/128K context rows,
the xhigh Qwen default, and the requirement for explicit authorization before a
live local run.

Machine-local configuration, account data, artifact metadata, logs, screenshots,
benchmark results, and recovery instructions remain private.
